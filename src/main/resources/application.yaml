spring:
  application:
    name: api-chat-ai

  ai:
    openai:
      api-key: ${GROQ_AI_API_KEY}
      base-url: ${GROQ_AI_URL}
      chat:
        options:
          model: ${GROQ_AI_MODEL}
          temperature: 0.5
      connection-timeout: 30s
      read-timeout: 60s